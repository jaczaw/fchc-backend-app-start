


#security:
#  ignored: /**

logging:
  file:
    name: my.log
spring:
  datasource:
    url: jdbc:postgresql://db:5432/dev
    username: jaczaw
    password: sekret
  profiles:
    active: default #@spring.profiles.active@
  liquibase:
    change-log: classpath:/db/changelog/changelog-master.xml
    enabled: true
  output:
    ansi:
      enabled: ALWAYS
#  security:
#    user:
#      name: user
#      password: user
  jpa:
    open-in-view: false
    hibernate:
      dll-auto: none
#      naming:
#        implicit-strategy: org.hibernate.boot.model.naming.ImplicitNamingStrategyLegacyJpaImpl
#        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    show-sql: true
server:
  port : 8181
app:
  datasource:
    jdbc-url: jdbc:postgresql://db:5432/dev
    username: jaczaw
    password: sekret
    pool-size: 30


jwt:
  expirationTime: 3600000 # 1 godzina
  secret: asdsalqrk1232115dfas@!#!@sds # niezapomnij podmienić na produkcji

#actuator ujawnić wszystkie dostępne punkty końcowe Spring Boot Actuator.
management:
  endpoints:
    web:
      exposure:
        include: '*'